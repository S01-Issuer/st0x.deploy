name: Git is clean
on: [push]
jobs:
  git-clean:
    # Regenerates everything `script/Build.sol` generates (`run()`, which
    # freezes nothing), formats, and fails if any committed file drifted.
    uses: rainlanguage/rainix/.github/workflows/rainix-copy-artifacts.yaml@main
