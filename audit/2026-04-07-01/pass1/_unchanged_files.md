# Pass 1 — Unchanged files (carried forward from prior audit)

The following source files are unchanged in the corporate-actions PR stack relative to `main`. They were audited in `audit/2026-03-19-01/` (and earlier runs), and all LOW+ findings from that pass were triaged to FIXED, DOCUMENTED, or DISMISSED. Re-running Pass 1 against unchanged code with no new context would produce no new findings, so this audit run carries those files' Pass 1 results forward by reference rather than re-writing them.

Files:

- `src/concrete/StoxReceipt.sol` — A02 — see `audit/2026-03-19-01/pass1/StoxReceipt.md` (no LOW+ findings remained open)
- `src/concrete/StoxWrappedTokenVault.sol` — A04 — see `audit/2026-03-19-01/pass1/StoxWrappedTokenVault.md`
- `src/concrete/StoxWrappedTokenVaultBeacon.sol` — A05 — see `audit/2026-03-19-01/pass1/StoxWrappedTokenVaultBeacon.md` (A06-1 DOCUMENTED in 2026-03-19-01 triage)
- `src/concrete/authorize/StoxOffchainAssetReceiptVaultAuthorizerV1.sol` — A06 — empty inheriting wrapper
- `src/concrete/authorize/StoxOffchainAssetReceiptVaultPaymentMintAuthorizerV1.sol` — A07 — empty inheriting wrapper
- `src/concrete/deploy/StoxOffchainAssetReceiptVaultBeaconSetDeployer.sol` — A08 — see `audit/2026-03-19-01/pass1/`
- `src/concrete/deploy/StoxUnifiedDeployer.sol` — A09 — A02-1 in 2026-03-19-01 was FIXED (deps array)
- `src/concrete/deploy/StoxWrappedTokenVaultBeaconSetDeployer.sol` — A10 — see `audit/2026-03-19-01/pass1/`
- `src/generated/*.pointers.sol` — A11–A19 — auto-generated bytecode constants (no functions, no logic surface). Each is a verbatim output of `script/BuildPointers.sol` against the corresponding contract's compiled creation code. Reviewing them is equivalent to reviewing the contract they pin; the pinning is verified at fork-test time via codehash assertions in `test/src/lib/LibProdDeployV2.t.sol` and friends.
- `src/lib/LibProdDeployV1.sol` — A24 — pure constants (addresses, codehashes, creation bytecodes) recording the V1 Base deployment audit trail. CLAUDE.md mandates these are immutable and not removed even when unreferenced (line 55).
- `src/lib/LibProdDeployV2.sol` — A25 — same as A24, V2 Zoltu addresses. Imports `DEPLOYED_ADDRESS` / `BYTECODE_HASH` constants from the generated pointer files and exposes them under named constants.

The unchanged files were verified unchanged via `git diff --stat main...HEAD -- src/`, which shows only:

```
src/concrete/StoxCorporateActionsFacet.sol         | 110 ++++++++
src/concrete/StoxReceiptVault.sol                  |  81 +++++-
src/interface/ICorporateActionsV1.sol              |  77 ++++++
src/lib/LibCorporateAction.sol                     | 223 +++++++++++++++
src/lib/LibCorporateActionNode.sol                 | 110 ++++++++
src/lib/LibERC20Storage.sol                        |  59 ++++
src/lib/LibRebase.sol                              |  70 +++++
src/lib/LibStockSplit.sol                          |  35 +++
src/lib/LibTotalSupply.sol                         | 163 +++++++++++
```

No new findings in Pass 1 for the unchanged files.
